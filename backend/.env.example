MYINSTA_DATABASE_PATH=./data/myinsta.sqlite3
MYINSTA_DOWNLOAD_DIR=./data/downloads
MYINSTA_AUDIO_DIR=./data/audio
MYINSTA_LIBRARY_DIR=./data/library
MYINSTA_WIKI_DIR=./data/mywiki
MYINSTA_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
MYINSTA_WHISPER_MODEL=base
# 0 disables the YouTube duration guard. Set seconds here to re-enable it.
MYINSTA_MAX_YOUTUBE_DURATION_SECONDS=0

# Brave Search API — free tier 2000 queries/month
# Get your key at: https://brave.com/search/api/
MYINSTA_BRAVE_SEARCH_API_KEY=your_key_here

# Instagram cookies for yt-dlp (Netscape cookies.txt)
INSTAGRAM_COOKIES_FILE=/home/nasser/.config/myinsta/instagram_cookies.txt

# ── YouTube (proven VPS setup — see backend/README.md) ───────────────────────
# MUST be a Netscape cookies.txt that includes LOGIN_INFO (not SID-only).
# Good export is often ~3KB YouTube-only; a 190KB "all sites" dump is often wrong.
# Verify: grep -c LOGIN_INFO /path/to/youtube_cookies.txt  → must be >= 1
YOUTUBE_COOKIES_FILE=/home/nasser/.config/myinsta/youtube_cookies.txt

# Local only (leave empty on VPS): chrome | edge | firefox
YOUTUBE_COOKIES_FROM_BROWSER=

# Server also needs: Node.js >= 22, FFmpeg, pip install -U "yt-dlp[default]" yt-dlp-ejs
# App calls: yt-dlp --js-runtimes node --remote-components ejs:github --cookies ...
#
# VPS API port is 8010 (systemd), not 8000:
#   curl -sS http://127.0.0.1:8010/health/youtube
#
# Optional proxy if the VPS IP is blocked:
# YOUTUBE_PROXY=http://user:pass@host:port
