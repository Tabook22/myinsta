# MyInsta Frontend

React + Vite frontend for submitting Instagram URLs and viewing video transcript results.

## Setup

```bash
cd frontend
npm install
npm run dev
```

The app expects the backend at `http://localhost:8000` by default.
Set `VITE_API_BASE_URL` if needed.
